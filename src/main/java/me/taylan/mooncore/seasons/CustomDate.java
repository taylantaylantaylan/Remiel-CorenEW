package me.taylan.mooncore.seasons;

import me.taylan.mooncore.MoonCore;
import org.bukkit.scheduler.BukkitRunnable;

public class CustomDate {


/*
    private MoonCore plugin;
    private CustomDateMethods customDateMethods;

    public CustomDate(MoonCore plugin) {
        this.plugin = plugin;
        this.customDateMethods = plugin.getCustomDateMethods();
    }


    @Override
    public void run() {
        if (customDateMethods.getCustomDay() < 360) {
            if (customDateMethods.getCustomTime() < 24) {
                String doubleAsString = String.valueOf(customDateMethods.getCustomTime());
                int indexOfDecimal = doubleAsString.indexOf(".");
                if (doubleAsString.substring(indexOfDecimal).equals(".5")) {
                    customDateMethods.setCustomTime(0.5);
                } else {
                    customDateMethods.setCustomTime(0.1);
                }
            } else if (customDateMethods.getCustomTime() >= 24) {
                customDateMethods.setCustomDay(1);
                customDateMethods.resetTime();
                if (customDateMethods.getCustomDay() % 60 == 0) {
                    customDateMethods.changeSeason();
                }
            }
        } else if (customDateMethods.getCustomDay() >= 360) {
            customDateMethods.setCustomYear(1);
            customDateMethods.resetDay();
        }
    }


}

 */
}


